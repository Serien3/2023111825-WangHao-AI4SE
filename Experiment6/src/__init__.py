"""实验六：改进针对大模型生成代码的代码审查。

在「只优化输入(上下文+Prompt)、不改模型」前提下，用假设驱动的
「上下文阶梯 L0–L4 + Prompt 消融(P5/P6)」弄清 AI 代码审查性能下降的来源，
并用可被观测的指标（per-class P/R + 混淆矩阵 + balanced accuracy；LLM-judge 多维打分）
证明改进是否有效。实验四/五的下游消费者。
"""
